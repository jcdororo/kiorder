node_modules
.env
.env.local
.DS_Store
.mcp.json
backend/dist/
docs/*
!docs/images/
구상.txt
_workspace/
_workspace_*/

# 외부 스킬(skills.sh로 설치). 내용은 추적하지 않고 skills-lock.json으로만 재현한다.
# .claude/skills/ 아래 설치본은 .agents/ 를 가리키는 Windows Junction이라,
# 추적하면 같은 내용이 두 벌 커밋된다. 직접 만든 kiorder-* 스킬만 추적한다.
.agents/
.claude/skills/*
!.claude/skills/kiorder-*/